# Classes Exercise 1

1. Create a network device class. The class should have fields for ip_addr, username, password, serial_number, model, vendor, uptime and os_version.
2. Assign at least the ip_addr, username, and password field to the object using `__init__`.
3. Create four different network device objects using this class.
